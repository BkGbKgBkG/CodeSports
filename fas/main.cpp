#include <bits/stdc++.h>
#define ll long long
#define  ii pair<int, int>
#define re return
#define mofile freopen("", "r", stdin);
#define dongfile freopen("", "w", stdout);

using namespace std;

int main() {
    mofile
    dongfile
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    return 0;
}
